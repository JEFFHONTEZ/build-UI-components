import { Calendar } from "@workspace/ui/components/calendar"

export default function CalendarPreview() {
  return (
    <div className="p-10">
      <h1 className="mb-6 text-lg font-semibold">Calendar</h1>
      {/* TODO: replace with a real composed example for Calendar */}
      <Calendar />
    </div>
  )
}
